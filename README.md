# Vanmoi

服务器监控主控端，使用 Rust Axum + Vue3 + PostgreSQL 构建。

## 功能特性

- 🖥️ **服务器监控** - 实时查看 CPU、内存、磁盘、网络等状态
- 📊 **仪表盘** - 直观的服务器状态概览
- 🔔 **通知系统** - 支持 Telegram、邮件、Webhook 告警
- 🌐 **Ping 监控** - 服务器延迟监控任务
- 🔐 **认证系统** - 用户登录和会话管理
- 🐳 **Docker 部署** - 一键 Docker Compose 部署

## 快速开始

### Docker 部署

```bash
# 克隆项目
git clone https://github.com/your-repo/vanmoi.git
cd vanmoi

# 启动服务
docker compose up -d

# 查看日志
docker logs vanmoi
```

访问 `http://localhost:8080` 使用服务。

默认管理员账户：
- 用户名: `admin`
- 密码: `changeme`

### 开发环境

1. 启动数据库：
```bash
docker compose up db -d
```

2. 启动后端：
```bash
cargo run
```

3. 启动前端（另一终端）：
```bash
cd web
npm install
npm run dev
```

## Agent 接入

参见 [Agent 通信协议文档](docs/agent-protocol.md)

## 项目结构

```
vanmoi/
├── src/                # Rust 后端
│   ├── api/            # API 路由
│   ├── db/             # 数据库层
│   ├── middleware/     # 中间件
│   ├── notifier/       # 通知系统
│   └── ws/             # WebSocket
├── web/                # Vue3 前端
│   ├── src/
│   │   ├── views/      # 页面组件
│   │   ├── components/ # 通用组件
│   │   ├── stores/     # 状态管理
│   │   └── api/        # API 客户端
├── Dockerfile
└── compose.yml
```

## 环境变量

| 变量             | 说明                | 默认值                                           |
| ---------------- | ------------------- | ------------------------------------------------ |
| `DATABASE_URL`   | PostgreSQL 连接地址 | `postgres://vanmoi:vanmoi@localhost:5432/vanmoi` |
| `LISTEN_ADDR`    | 监听地址            | `0.0.0.0:8080`                                   |
| `JWT_SECRET`     | JWT 密钥            | 随机生成                                         |
| `ADMIN_USERNAME` | 初始管理员用户名    | `admin`                                          |
| `ADMIN_PASSWORD` | 初始管理员密码      | 随机生成                                         |

## License

MIT
